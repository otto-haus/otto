# Channels

Communication surfaces for Otto. Files remain source of truth; channels are reachability only.

v1 ships Discord as the primary off-desktop backend (see `docs/channels.md`). Enable a channel only after bot credentials and approval gates are configured.
