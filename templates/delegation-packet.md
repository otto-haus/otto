# Delegation packet: <task>

The charter stays owned by the dispatcher. The delegate executes one Worker step.

## Objective
<what success looks like for this step>

## Constraints
<runtime, time, style, do-not-touch>

## Relevant files / paths
- <path>

## Acceptance criteria
1. <criterion> -> proof: <receipt>

## Approval gates (delegate must ASK first)
- send/post/publish, spend, deploy, merge to protected main, delete important data,
  credential/security changes, customer/live-file access

## Expected output path
<where to write the result + receipts>

## What NOT to do
- Do not redefine the objective.
- Do not approve or perform one-way-door actions.
- Do not expand scope.
