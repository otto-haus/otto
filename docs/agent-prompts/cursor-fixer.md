# Prompt — Cursor fixer alias

Use `docs/agent-prompts/composer-implementer.md` for the implementation agent prompt.

The implementation lane is now called **Composer** because Sebastian wants Composer 2.5 Fast as the default implementer. This file remains only as a compatibility alias for older references to Cursor.
