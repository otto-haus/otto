# Icon prompts (local only)

The gpt-image-2 prompt pack and raw PNG masters are **not** in the public repo.

Keep them in your local design mirror (e.g. Dropbox `This Cycle/otto/`: `otto-icon-prompts.md`, `iconography/`).

Public icon reference: `docs/design/icons/` and shipped vectors in `apps/desktop/src/components/icon-art.ts`.
