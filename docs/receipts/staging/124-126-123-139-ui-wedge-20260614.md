# UI wedge batch — 124 / 126 / 123 / 139

**Branch:** `ship/functional-labs` · **Date:** 2026-06-14

## Verified

```sh
bun run verify:v0
# 5/5 pass (180 unit tests, desktop typecheck)
```

## Changes

| Ticket | Proof |
|--------|-------|
| **124** Receipts first-class | `ReceiptCard` in `Panes.tsx`; authority filter; `ReceiptInlineCard` uses `receiptsCopy`; detail authority + evidence unchanged |
| **126** Ratification moment | `toastCopy.behaviorUpdated*` + **Open receipt** toast action on Curation accept |
| **123** Correction loop | Primary **Correct this** on all propose-eligible messages; modal copy from `chatCopy`; permission deny path unchanged |
| **139** Labs shells | `LabsBlockedShell`; `ComingSoonSurface` + App loading use `labsCopy`; Cognee/pgvector/channels blocked copy; no `OTTO_*` in primary pane copy |

## Files touched

- `apps/desktop/src/copy/surfaces.ts`
- `apps/desktop/src/surfaces/Panes.tsx`
- `apps/desktop/src/surfaces/Chat.tsx` (MessageActions only — no thread/permission layout)
- `apps/desktop/src/chat/ProposeCorrectionModal.tsx`
- `apps/desktop/src/components/ui/{MessageActions,ReceiptInlineCard}.tsx`
- `apps/desktop/src/components/Toast.tsx`
- `apps/desktop/src/labs/{ComingSoonSurface,LabsBlockedShell}.tsx`
- `apps/desktop/src/App.tsx`
- `apps/desktop/src/styles.css`

## Manual (optional)

- Labs off → Knowledge/Channels → Coming soon shell
- Labs on → Cognee disabled → Labs blocked shell in Knowledge
- Accept proposal → Behavior updated toast → Open receipt
