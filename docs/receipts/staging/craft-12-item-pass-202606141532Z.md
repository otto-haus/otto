# 12-item craft pass — staging (Phase 0)

Date: 2026-06-14  
Branch: `ship/functional-labs` @ `404f2f0`  
App: `/Applications/otto-staging.app` (display name **otto staging**)  
Live `/Applications/otto.app` **not** redeployed (process may still be running from prior session).

## Phase 0 gates

| Gate | Result | Evidence |
|------|--------|----------|
| `bun run verify:v0` | pass | 208 tests + desktop typecheck, 2026-06-14 |
| `deploy-staging.sh` | pass | mtime 2026-06-14 08:31:30, pid 37589, port 9445 |
| Window title | pass | `CFBundleDisplayName` = `otto staging` |
| CDP proof bundle | partial | `otto-staging-proof-capture.cjs` timed out waiting for `.commandStation` (runtime not ready in isolated staging profile) |

## Checklist (plan Phase 0 table)

| # | Item | Status | Proof |
|---|------|--------|-------|
| 1 | Logo not squeezed | pass (source) | `OttoMark.tsx` explicit px size; `.chat__avatar` grid + `img.ottoMark` bounds |
| 2 | Social / OG image | pass | Brand assets under `site/` + `docs/brand/`; Dropbox social path out of repo scope |
| 3 | Local image attachments | pass (source) | `withAttachments()` appends path list to message body (`Chat.tsx`) |
| 4 | Busy copy | pass | `chatCopy.workingPulse` = `thinking…` (`surfaces.ts`) |
| 5 | One primary agent | pass | No fleet UI; single runtime thread model |
| 6 | Skills not agents | pass | Sidebar labels Skills/Knowledge; copy in `surfaces.ts` |
| 7 | Chat header clean | pass (source) | No MemFS / CONNECTED / agent-local strings in `Chat.tsx` |
| 8 | Model/effort layout | pass (source) | `.promptControls` above textarea in `promptCompose`; no cli/tools footer |
| 9 | System nav icons | pass (source) | Distinct icons: owl / theme / plus / send (`Sidebar.tsx`, ticket 057) |
| 10 | Send icon | pass (source) | `Icon.send` traced SVG in composer |
| 11 | Toast / proposal UX | pass (source) | `useToast` on proposal create; `ProposeCorrectionModal` wired |
| 12 | Staging vs live | pass | Deploy script targets only `otto-staging.app`; proof doc from staging bundle |

## Fixes applied this pass

None required from checklist — integration branch already contains craft items from tickets 081/057/031.

## Open (not blockers for Phase 0)

- Full-route CDP screenshots: re-run after Letta reachable in staging (`LETTA_BASE_URL` / agent id in staging plist).
- Promote `v0.1.3` to **Latest** on GitHub before README badge URL serves the new MP4 (asset uploaded; pre-release only).

## Commands

```sh
bun run verify:v0
bash apps/desktop/scripts/deploy-staging.sh
NODE_PATH=$HOME/.codex/admin/node_modules \
  OTTO_GIT_HEAD=$(git rev-parse --short HEAD) \
  node scripts/otto-staging-proof-capture.cjs
```
