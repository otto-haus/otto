# Issue #651 — Preview Mode product contract

- Added `docs/v1/preview-mode.md` — purpose, states, sandbox boundary, persistence, Ship vs Labs, downstream slices.
- Linked from Chat surface contract (layout + required actions).
- Doc-only; no staging refresh required.
