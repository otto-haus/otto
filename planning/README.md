# Otto planning mirrors

Canonical working copies live in Dropbox. These paths are repo mirrors for review, CI, and agent context.

| Path | Source |
|---|---|
| `planning/hq-tickets/` | `~/Library/CloudStorage/Dropbox/HQ/Otto Tickets` |
| `planning/lane-tickets/` | `~/Library/CloudStorage/Dropbox/This Cycle/otto/tickets` |
| `docs/v1/` | `~/Library/CloudStorage/Dropbox/This Cycle/otto/v1` |

Folder location is ticket status for HQ queue (`root`, `_Done`, `_Parked`, `_InReview`). Lane tickets use inline `Status:` in each file.

Do not treat chat as state. Proof lives in the ticket files.
